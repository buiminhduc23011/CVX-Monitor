<UserControl x:Class="CVX_QLSX.App.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Báo cáo Kế hoạch Sản xuất" 
                   FontSize="22" FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,16"/>

        <!-- Filter -->
        <materialDesign:Card Grid.Row="1" UniformCornerRadius="10" Margin="0,0,0,16"
                             Padding="16" Background="White" materialDesign:ElevationAssist.Elevation="Dp1">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <DatePicker Grid.Column="0"
                                materialDesign:HintAssist.Hint="Từ ngày"
                                materialDesign:HintAssist.IsFloating="True"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                SelectedDate="{Binding FromDate}"
                                Margin="0,0,12,0"/>

                    <DatePicker Grid.Column="1"
                                materialDesign:HintAssist.Hint="Đến ngày"
                                materialDesign:HintAssist.IsFloating="True"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                SelectedDate="{Binding ToDate}"
                                Margin="0,0,12,0"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Command="{Binding LoadReportCommand}"
                                Content="Tải báo cáo" Height="40" Padding="20,0"/>
                        
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                Command="{Binding ExportToExcelCommand}"
                                Margin="12,0,0,0" Height="40" Padding="20,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="MicrosoftExcel" Width="20" Height="20" VerticalAlignment="Center"/>
                                <TextBlock Text="Xuất Excel" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
                
                <TextBlock Text="{Binding ExportStatus}" Foreground="#666" Margin="0,12,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Summary Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" UniformCornerRadius="10" Margin="0,0,4,0"
                                 Padding="12" Background="White">
                <StackPanel>
                    <TextBlock Text="Tổng Kế hoạch" Foreground="#888"/>
                    <TextBlock Text="{Binding TotalPlans}" FontSize="24" FontWeight="Bold" Foreground="#1976D2"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" UniformCornerRadius="10" Margin="4,0,4,0"
                                 Padding="12" Background="White">
                <StackPanel>
                    <TextBlock Text="Hoàn thành" Foreground="#888"/>
                    <TextBlock Text="{Binding CompletedPlans}" FontSize="24" FontWeight="Bold" Foreground="#43A047"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2" UniformCornerRadius="10" Margin="4,0,4,0"
                                 Padding="12" Background="White">
                <StackPanel>
                    <TextBlock Text="Đã hủy" Foreground="#888"/>
                    <TextBlock Text="{Binding CancelledPlans}" FontSize="24" FontWeight="Bold" Foreground="#E53935"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="3" UniformCornerRadius="10" Margin="4,0,0,0"
                                 Padding="12" Background="White">
                <StackPanel>
                    <TextBlock Text="Tổng SL thực tế" Foreground="#888"/>
                    <TextBlock Text="{Binding TotalActualQuantity, StringFormat=N0}" FontSize="24" FontWeight="Bold" Foreground="#8E24AA"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Plan DataGrid -->
        <materialDesign:Card Grid.Row="3" UniformCornerRadius="10"
                             Padding="16" Background="White" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Chi tiết Kế hoạch" FontSize="20" FontWeight="SemiBold"
                           Foreground="#333" Margin="0,0,0,12"/>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Plans}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          materialDesign:DataGridAssist.CellPadding="12 8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="12 8"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Width="50" IsReadOnly="True"
                                            Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, 
                                            Converter={StaticResource RowNumberConverter}}"/>
                        <DataGridTextColumn Header="Sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="SL Đặt" Binding="{Binding TargetQuantity, StringFormat=N0}" Width="80"/>
                        <DataGridTextColumn Header="Thực tế" Binding="{Binding CurrentQuantity, StringFormat=N0}" Width="80"/>
                        <DataGridTextColumn Header="Tạo lúc" Binding="{Binding CreatedAt, StringFormat=dd/MM HH:mm}" Width="100"/>
                        <DataGridTextColumn Header="Bắt đầu" Binding="{Binding StartedAt, StringFormat=dd/MM HH:mm}" Width="100"/>
                        <DataGridTextColumn Header="Kết thúc" Binding="{Binding CompletedAt, StringFormat=dd/MM HH:mm}" Width="100"/>
                        <DataGridTemplateColumn Header="Trạng thái" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="5,2" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E0E0E0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Running">
                                                        <Setter Property="Background" Value="#1976D2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                        <Setter Property="Background" Value="#43A047"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                        <Setter Property="Background" Value="#E53935"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontWeight="Medium">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Chờ"/>
                                                    <Setter Property="Foreground" Value="#666"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Running">
                                                            <Setter Property="Text" Value="Đang chạy"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                            <Setter Property="Text" Value="Xong"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                            <Setter Property="Text" Value="Đã hủy"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
